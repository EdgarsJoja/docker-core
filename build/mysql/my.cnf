[mysqld]
general_log = 1
general_log_file = /var/lib/mysql/general.log

secure-file-priv = NULL

# Need this to be able to do php requests to mysql 8
default-authentication-plugin = mysql_native_password
